{% extends 'base.html.twig' %}

{% block title %} Ajouter une adresse - La Boutique Française {% endblock %}

{% block content %}
    
    <h3> Gérer mes adresses  </h3>

    <p> C'est dans cette espace que vous allez pouvoir crée vos adresse. <a href="{{path('account_adress')}}"> Retour </a> </p>  
    
    {# {{form(form)}} cette présentation de twig est utiliser pour afficher les tableau mais ne permet pas de la manipuler. Pour cela on l'éclater #}

    {{ form_start(form) }} {#  début du formaulaire #}
        
        <div class="row">

            <div class="col-md-12">
                {{ form_row(form.name) }}{# form_row() permet d'intéragir avec chaque input du form en utilisant le nom donner a ceux-ci ex: form.name , form.lastname..#} 
            </div>
            <div class="col-md-6">
                {{ form_row(form.firstname) }} 
            </div>
            <div class="col-md-6">
                {{ form_row(form.lastname) }}  
            </div>
            <div class="col-md-6">
                {{ form_row(form.company) }} 
            </div>
            <div class="col-md-6">
                {{ form_row(form.address) }} 
            </div>
            <div class="col-md-6">
                {{ form_row(form.code_postal) }} 
            </div>
            <div class="col-md-6">
                {{ form_row(form.city) }} 
            </div>
            <div class="col-md-6">
                {{ form_row(form.pays) }} 
            </div>
            <div class="col-md-6">
                {{ form_row(form.phone) }} 
            </div>
        
        </div>
        
    {{ form_end(form) }} {# fin du formaulaire #}
{% endblock %}
